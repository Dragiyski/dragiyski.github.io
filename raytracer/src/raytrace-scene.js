class RaytraceScene {
    static #objectType = [];
    #objects;
    #data;

    /**
     * The scene must be build prior to rendering.
     * The fragment shader will go into single pass
     */
    async build() {
    }
}
